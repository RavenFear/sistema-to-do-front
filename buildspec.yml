version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: 18
        commands:
            - npm ci

    build:
        commands:
            - npm run build -- --configuration production
            - chmod +x scripts/restart_apache.sh

artifacts:
    files:
        - appspec.yml
        - scripts/**
        - dist/booster-ng/**/*
